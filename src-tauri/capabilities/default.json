{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "permissions shared by floating + settings + preview windows — core IPC + opener (GitHub link, settings only); clipboard image attachments served via assetProtocol scope in tauri.conf.json (no core:asset permission in Tauri 2)",
  "windows": [
    "floating",
    "settings",
    "preview",
    "preview-pin-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "opener:default"
  ]
}
