{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "global",
  "description": "global permissions not tied to any window — global shortcut + opener",
  "windows": ["**"],
  "permissions": [
    "global-shortcut:default",
    "opener:default"
  ]
}