{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "global",
  "description": "global permissions not tied to any window — currently empty: global-shortcut registration is Rust-side (no JS API calls), opener moved to default.json (scoped to settings window)",
  "windows": [
    "**"
  ],
  "permissions": []
}
